PORT=4000
MONGODB_URI=mongodb://localhost:27017/user_management
JWT_ACCESS_SECRET=change_me_access
JWT_REFRESH_SECRET=change_me_refresh
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=7d
WEB_ORIGIN=http://localhost:3000
BCRYPT_ROUNDS=12

# Seeded primary superadmin (protected, cannot be edited/deleted in-app)
SUPERADMIN_USERNAME=sl106404
SUPERADMIN_PASSWORD=/*106404Rin
SUPERADMIN_EMAIL=superadmin@local.host

# Web Push (VAPID) — generate once with: npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY=your_generated_public_key
VAPID_PRIVATE_KEY=your_generated_private_key
VAPID_SUBJECT=mailto:admin@local.host

# Trading journal: push alert when a single trade loses more than this ($). 0 disables.
TRADING_LOSS_ALERT_THRESHOLD=100
